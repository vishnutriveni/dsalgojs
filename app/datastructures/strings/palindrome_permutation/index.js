// Check if a string is palindrome permutation. 
const isPalindrome = (string) => {

    // Palindrome strings will always be of odd number length.
    if(string.length %2 !== 1) {
        return false;
    }

    const hashMap = {};
    // if()
}